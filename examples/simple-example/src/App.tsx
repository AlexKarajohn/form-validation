// Releasing of scoped packages isn't free, thus using different names for developing & usage
// Also in packages\password\package.json:name=@form-validation/password then perform npm install in root folder for the namespaces to take effect
// import { PasswordForm } from "@form-validation/password";
// import { PasswordForm } from "akaragiannis-password-form-validation";

function App() {
  // return <PasswordForm />;
  return <div></div>;
}

export default App;
